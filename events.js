/*const eventEmitter = require("events");
const em = new eventEmitter();
const fs = require("fs");
const rr = fs.createReadStream("./dummy.json"); //starts a event of readinf a file in bytes
rr.on("data", (data) => {
  console.log({ data });
});
rr.on("end", (data) => {
  //ending the uopper event
  console.log({ data });
});*/

// em.on("demo", (data) => {
//   console.log("Event Called...!", data);
// });
// setTimeout(() => {
//   em.emit("demo", { name: "dummy" });
// }, 5000);
